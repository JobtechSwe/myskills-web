const colors = {
  lassekongo: 'dodgerblue',
}

const sizes = [0, 16, 24, 32]

const typography = {
  default: 'Arial',
}

const space = {
  none: sizes[0],
  small: sizes[1],
  medium: sizes[2],
  big: sizes[3],
}

const theme = {
  colors,
  space,
  typography,
}

export default theme

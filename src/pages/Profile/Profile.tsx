import React from 'react'
import { RouteComponentProps } from '@reach/router'

const Profile: React.FC<RouteComponentProps> = props => (
  <div>This is the protected profile page!</div>
)

export default Profile
